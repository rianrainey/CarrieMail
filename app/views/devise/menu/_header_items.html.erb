<% if controller_name != 'sessions' && controller_name != 'registrations' %>
<div id="menu-items">
	<% if anyone_signed_in? %>
	<ul>
	<li><%= link_to current_or_guest_user.name, edit_user_registration_path(current_or_guest_user) -%></li>
	<li><%= link_to 'Logout', destroy_user_session_path -%></li>
	</ul>
	<% else %>
	<ul>
	<li><%= link_to 'Sign In', new_user_session_path -%></li>
	</ul>
	<% end %>
</div>
<div id="utility-nav">
	<ul>
		<li>Gallery</li>
		<li>Link</li>		
		<li>Blog</li>
		<li>Contact Us</li>	
	</ul>
</div><!--#utility-nav-->
<% end %>
